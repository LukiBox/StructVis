# StructVis dependencies
# UI + 3D
PySide6>=6.6
pyvista>=0.43
pyvistaqt>=0.11
pyqtgraph>=0.13

# Numerics
numpy>=1.26
scipy>=1.11

# Report (PDF + charts)
reportlab>=4.0
matplotlib>=3.8

# AI structural review (local, offline)
ollama>=0.3

# Optional: high-fidelity spanwise loads by re-running the VLM
# aerosandbox>=4.2

# Dev / packaging
pytest>=8.0
pyinstaller>=6.0

# External solver (NOT a pip package): CalculiX `ccx`.
# Provide it one of three ways (see README / Help > Solver status):
#   1. drop ccx(.exe) into structvis/resources/bin/
#   2. set STRUCTVIS_CCX to its full path
#   3. install CalculiX/PrePoMax and add ccx to PATH
